name: get env vair
on: 
#  push:
#    branches:
#      actions-demo
  workflow_dispatch:
env:
  idd: 'suman'

jobs:
  printt:
    runs-on: ubuntu-latest
    steps:
       - name: hello
         run: echo "$idd"
      
         
      
  
